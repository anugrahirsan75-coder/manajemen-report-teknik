node_modules
.next
.env.local
out
dist
*.log

tsconfig.tsbuildinfo
server.log

# RAB master (sumber katalog, di-generate jadi src/lib/katalog/katalogSeed.json)
RAB_MASTER*.xlsx

# output generator (file live katalog dll — artifact, jangan commit)
output/

# Arsip dokumen pengadaan (data operasional, bukan kode)
/SPBJ/
public/backup/

# model bahasa tesseract.js (diunduh sendiri saat skrip OCR jalan)
ind.traineddata
*.traineddata

# Berkas pendukung audit docking (438 MB PDF arsip cabang, bukan kode)
/Persiapan Audit/

# Surat acuan e-office (memuat QR tanda tangan asli pejabat, bukan kode)
/Template Surat/

# basis data lokal: suntingan rekap dokumentasi, milik laptop ini saja.
# Hanya berkas itu yang diabaikan — folder data/ juga memuat hargaIndex.json
# yang memang ikut repo.
data/dokumentasi-ubahan.json

# gambar tanda tangan & stempel — milik orangnya, tidak ikut ke penyimpanan kode
data/ttd/

# daftar awak lengkap dengan NIK — data pribadi, hanya di laptop
data/abk-lengkap.json
